interface:
  display_name: "ZDecision Recall"
  short_description: "Confirm and apply formal decisions in this task"
  default_prompt: "Use $zdecision to show the confirmation card; after I click enable, wait for my next native message and apply the complete handoff before affected work."
policy:
  allow_implicit_invocation: false
